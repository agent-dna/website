Drop these brand files here (replace the placeholders):

  ab.png            — AgentDNA mascot/character
  logo-blue.png     — Horizontal AgentDNA logo, navy blue (used on light sections)
  logo-white.png    — AgentDNA logo, white version (used on navy sections)
  logo-mark.png     — Square mark / favicon-style logo

The site is wired to read them from /assets/<name>.png. If a file is missing,
a CSS fallback (initials/SVG monogram) renders in its place so layout is preserved.
